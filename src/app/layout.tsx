import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

// FIX: Updated from boilerplate "Create Next App" to actual app metadata
export const metadata: Metadata = {
  title: "SubSavvy.ai — Stop Wasting Money on Unwatched Subscriptions",
  description: "The intelligent tracker that analyzes your streaming watch time and tells you exactly which subscriptions to cancel.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}